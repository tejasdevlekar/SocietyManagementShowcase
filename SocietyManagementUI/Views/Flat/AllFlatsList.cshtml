@using Common.Models
@model List<AllFlatsListViewModel>

<h1 align="center" style="font-family: Palace Script MT;font-size: 87px;color: #6a6a6a;">
    Flat Management System
</h1>

<table>
    <tr>
        <td>
            Flat No
        </td>
        <td>
            Area (sq ft)
        </td>
        <td>
            Maintenance Charge
        </td>
        <td>
            Wing Name
        </td>
    </tr>
    @if (Model.Count > 0)
    {
        @foreach (AllFlatsListViewModel flat in Model)
        {
            <tr>
                <td>
                    @flat.FlatNo
                </td>
                <td>
                    @flat.AreaSqFt
                </td>
                <td>
                    @flat.MaintenanceCharge
                </td>
                <td>
                    @flat.WingName
                </td>
            </tr>
        }
    }
</table>

