namespace SocietyManagementShowcase.Common
{
    public enum StatusHealth
    {
        OK = 0,
        Maintenance,
        Issue,
        Critical
    }

    public enum SocietyRoleType
    {
        Member = 0,
        SubManager,
        Manager
    }
}